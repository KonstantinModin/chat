import React from "react";
import "./Header.css";

const Header = () => {
    return (
        <div className="Header">
            <h1>Real-time Chat with React / socket.io</h1>
            <h5>Try and use instant messages</h5>
        </div>
    );
};

export default Header;
